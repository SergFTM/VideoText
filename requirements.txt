yt-dlp[default]>=2024.12.0
curl-cffi>=0.7.0,<0.12
anthropic>=0.40.0
supadata>=1.6.0
python-dotenv>=1.0.0
prisma>=0.15.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.20  # required by FastAPI for file uploads (cookies.txt)
# Phase 1 — live retransmission
faster-whisper>=1.2.0
nvidia-cublas-cu12  # CUDA BLAS runtime, required by faster-whisper on GPU (Windows)
nvidia-cudnn-cu12   # CUDA DNN runtime
reportlab>=4.4.0
# System dep: ffmpeg (install via `winget install Gyan.FFmpeg` on Windows)
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
